// https://docs.microsoft.com/es-es/dotnet/csharp/language-reference/operators/
// los operadores con mayor prioridad
// se evalúan antes que los operadores con menor prioridad

// En la tabla siguiente se muestran los operadores de C# 
// desde la precedencia más alta a la más baja.
// Los operadores de cada fila comparten la prioridad.

/*
Operadores	                                        Categoría o nombre
-------------------------------------------------------------------------
x.y, f(x), a[i], x?.y, x?[y], x++, x--, x!,
new, typeof, checked, unchecked, default,           Principal
nameof, delegate, sizeof, stackalloc, x->y	

+x, -x, !x, ~x, ++x, --x, ^x, (T)x, await,
&x, *x, true and false	                            Unario

x..y	                                            Intervalo

switch	                                            Expresión switch

x * y, x / y, x % y	                                Multiplicativo

x + y, x – y	                                    Aditivo

x << y, x >> y	                                    Shift

x < y, x > y, x <= y, x >= y, is, as	            Comprobación de tipos y relacional

x == y, x != y	                                    Igualdad

x & y	                                            Operador lógico booleano AND u operador lógico bit a bit AND

x ^ y	                                            Operador lógico booleano XOR u operador lógico bit a bit XOR

x | y	                                            Operador lógico booleano OR u operador lógico bit a bit OR

x && y	                                            AND condicional

x || y	                                            OR condicional

x ?? y	                                            Operador de uso combinado de null

c ? t : f	                                        Operador condicional

x = y, x += y, x -= y, x *= y, x /= y, x %= y,
x &= y, x |= y, x ^= y, x <<= y, x >>= y,           Asignación y declaración lambda
x ??= y, =>
*/
